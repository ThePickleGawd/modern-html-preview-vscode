{
  "name": "improved-html-preview",
  "displayName": "Improved HTML Preview",
  "description": "Preview workspace HTML files and local or remote HTTP/HTTPS URLs inside VS Code with browser-like behavior.",
  "version": "0.0.1",
  "publisher": "local",
  "license": "MIT",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:improved-html-preview.openHtml",
    "onCommand:improved-html-preview.openUrl",
    "onCommand:improved-html-preview.openUrlAtCursor"
  ],
  "main": "./dist/extension.js",
  "extensionKind": [
    "workspace"
  ],
  "contributes": {
    "commands": [
      {
        "command": "improved-html-preview.openHtml",
        "title": "Open HTML Preview",
        "category": "Improved HTML Preview"
      },
      {
        "command": "improved-html-preview.openUrl",
        "title": "Open URL Preview",
        "category": "Improved HTML Preview"
      },
      {
        "command": "improved-html-preview.openUrlAtCursor",
        "title": "Open URL at Cursor",
        "category": "Improved HTML Preview"
      }
    ],
    "keybindings": [
      {
        "command": "improved-html-preview.openHtml",
        "key": "ctrl+shift+v",
        "mac": "cmd+shift+v",
        "when": "editorTextFocus && (resourceExtname == .html || resourceExtname == .htm)"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "check": "tsc --noEmit -p ./",
    "package": "vsce package --allow-missing-repository"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.2.1",
    "typescript": "^5.7.2"
  }
}
